import test from 'tape-promise/tape'

import Runner from '../index'
import Reporter from '../reporters/tdd'

import testCase from '../../tests/dummy/repeat-11'

const reporter = Reporter()

test('Repeat 10 - until condition', assert => {
  // todo
  assert.end()
})
